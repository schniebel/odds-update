apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: odds-compute-prod
  namespace: flux-system
spec:
  dependsOn:
    - name: infra-ops-crossplane-deploy
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./clusters/odds-compute/prod
  prune: true
  wait: true
  timeout: 5m0s
  kubeConfig:
    secretRef:
      name: odds-compute-prod-kubeconfig
