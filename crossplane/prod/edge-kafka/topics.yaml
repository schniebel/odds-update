
apiVersion: topic.kafka.crossplane.io/v1alpha1
kind: Topic
metadata:
  name: odds-updates-public-nfl-prod
spec:
  providerConfigRef:
    name: edge-kafka-prod
  partitions: 12
  replicationFactor: 3
  config:
    min.insync.replicas: "2"
    cleanup.policy: "delete"
    retention.ms: "43200000"      # 12h

---
apiVersion: topic.kafka.crossplane.io/v1alpha1
kind: Topic
metadata:
  name: odds-updates-public-nba-prod
spec:
  providerConfigRef:
    name: edge-kafka-prod
  partitions: 12
  replicationFactor: 3
  config:
    min.insync.replicas: "2"
    cleanup.policy: "delete"
    retention.ms: "43200000"      # 12h

---
apiVersion: topic.kafka.crossplane.io/v1alpha1
kind: Topic
metadata:
  name: odds-updates-public-mlb-prod
spec:
  providerConfigRef:
    name: edge-kafka-prod
  partitions: 12
  replicationFactor: 3
  config:
    min.insync.replicas: "2"
    cleanup.policy: "delete"
    retention.ms: "43200000"      # 12h

---
# Edge DLQ for the Core→Edge replication hop (MM2/MSK Connect)
apiVersion: topic.kafka.crossplane.io/v1alpha1
kind: Topic
metadata:
  name: odds-updates-public-dlq-prod
spec:
  providerConfigRef:
    name: edge-kafka-prod
  partitions: 6
  replicationFactor: 3
  config:
    min.insync.replicas: "2"
    cleanup.policy: "delete"
    retention.ms: "604800000"     # 7d
